// Package prometheus Prometheus 观测 Provider Adapter（infrastructure 层）。
//
// 仅实现 metrics 与 alerts 能力（见 integration DefaultCapabilitiesForProvider）。
// 阶段 3 替换为 PromQL / Alertmanager API 客户端。
package prometheus
