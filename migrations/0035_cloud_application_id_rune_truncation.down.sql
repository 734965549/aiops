-- 0035 down: 人工回滚参考，不由 runner 自动执行。
-- 改写分支理论上可按同公式反推（rune 版 application_id -> 字节版），但字节版前缀取决于账号的 UTF-8
-- 字节布局，且 rune 版应用也可能由修复后的新同步创建（并非 0035 改写而来），反改写会把这类应用一并
-- 改回字节版，因此反改写必须限定在 0035 实际改写过的行——仅凭 SQL 无法区分，需对照升级前备份核对。
-- 合并分支（旧应用资源/规则已改写指向新应用、旧应用行已删除）同样需按备份反改写 application_id 并
-- 重建旧 asset_application 行。生产回滚应由 DBA 按审批流程显式执行。
